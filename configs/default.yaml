# SARE-HX Default Configuration
# These are the default energy weights and search parameters.

energy:
  weights:
    alpha: 1.0     # syntax error weight
    beta: 1.0      # constraint violation weight
    gamma: 1.0     # test failure weight
    delta: 0.5     # complexity penalty weight
    lambda: 0.3    # resource penalty weight
    mu: 0.2        # uncertainty penalty weight

search:
  algorithm: beam_search    # beam_search | mcts | hybrid
  beam_width: 8
  max_depth: 50
  kappa: 0.1               # heuristic weight in scoring
  budget_seconds: 30.0
  max_expansions: 10000
  utility_threshold: -0.5  # minimum transform utility

logging:
  output: logs/solves.jsonl
  verbose: false

# Phase activation flags
phases:
  graph: true          # Phase 1 ✓
  energy: true         # Phase 1 ✓
  transforms: true     # Phase 1 ✓
  search: true         # Phase 1 ✓
  verification: true   # Phase 1 ✓
  memory: true         # Phase 2 ✓ (activated)
  heuristics: true     # Phase 2 ✓ (activated)
  abstraction: true    # Phase 3 ✓ (activated)
  plasticity: true     # Phase 4 ✓ (activated)
  causal: true         # Phase 5 ✓ (activated)
  reflection: true     # Phase 6 ✓ (activated)
  curiosity: true      # Phase 7 ✓ (activated)
  curriculum: true     # Phase 8 ✓ (developmental learning)

brain_boot:
  memory: true
  metacognition: true
  knowledge: true
  world_model: true
  perception: true
  language: true
  social: true
  learning: true
  curriculum: true
  warmup_environment_discovery: true
  warmup_physics_session: true
  warmup_knowledge_ingestion: true
  warmup_transform_generator: true
  warmup_action_physics: true
  warmup_robustness_batch: true
  autostart_continuous_stream: true
  autostart_hippocampus: true
  seed_perception_priors: true
